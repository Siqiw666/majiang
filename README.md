# 青雀 · 麻将

一个无需安装依赖、打开浏览器即可游玩的单机麻将小游戏。

## 运行

直接打开 `index.html`，或在项目目录启动本地静态服务器：

```bash
python3 -m http.server 8080
```

然后访问 <http://localhost:8080>。

当前版本实现了四家轮流摸牌、出牌，以及“四组牌加一对将”的基础和牌判断。吃、碰、杠等规则将在后续版本加入。
